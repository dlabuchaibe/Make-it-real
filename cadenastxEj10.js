/* Dados dos números indicar si el primer número  es un número de teléfono fijo válido (7 dígitos) y 
el segundo número es un número de celular válido(10 dígitos). Los valores se deben capturar como 
cadenas de texto.
*/

var num1 = "3529242";
var num2 = "3106101010";

if (num1.length === 7 && num2.length === 10) {
    console.log("valido");
};
